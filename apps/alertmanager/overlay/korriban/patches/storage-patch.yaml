apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: alertmanager
  namespace: monitoring
spec:
  volumeClaimTemplates:
    - metadata:
        name: storage
      spec:
        storageClassName: nfs-holocron-general
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
