apiVersion: v1
kind: Service
metadata:
  name: harbor-valkey
  namespace: harbor
  labels:
    app.kubernetes.io/name: harbor-valkey
    app.kubernetes.io/component: redis
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: harbor-valkey
  ports:
    - name: valkey
      port: 6379
      targetPort: valkey
