apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-arr-complete
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: pv-arr-complete
  resources:
    requests:
      storage: 500Gi
