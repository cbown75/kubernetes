apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-arr-downloads
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: pv-arr-downloads
  resources:
    requests:
      storage: 500Gi
