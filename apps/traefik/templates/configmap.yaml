apiVersion: v1
kind: ConfigMap
metadata:
  name: traefik-config
  labels:
    app: traefik
data:
  traefik.yaml: |
    api:
      dashboard: true
    entryPoints:
      web:
        address: ":80"
      websecure:
        address: ":443"
    providers:
      kubernetesCRD: {}
