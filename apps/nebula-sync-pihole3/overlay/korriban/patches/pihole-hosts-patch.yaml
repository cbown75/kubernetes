apiVersion: apps/v1
kind: Deployment
metadata:
  name: nebula-sync-pihole3
spec:
  template:
    spec:
      initContainers:
        - name: setup-config
          env:
            - name: PIHOLE_PRIMARY_HOST
              value: "pihole3.home.cwbtech.net"
            - name: PIHOLE_REPLICA1_HOST
              value: "pihole4.home.cwbtech.net"
