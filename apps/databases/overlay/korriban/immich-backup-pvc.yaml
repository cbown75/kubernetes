apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-postgres-backups
  namespace: databases
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-holocron-databases
  resources:
    requests:
      storage: 10Gi
