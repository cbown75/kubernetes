apiVersion: v1
kind: Service
metadata:
  name: prometheus-node-exporter
  namespace: monitoring
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: metrics
      port: 9100
      targetPort: 9100
      protocol: TCP
  selector:
    app.kubernetes.io/name: prometheus-node-exporter
