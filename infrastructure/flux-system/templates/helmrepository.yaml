apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: {{ .Values.flux.helmRepository.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "flux-system.labels" . | nindent 4 }}
spec:
  interval: 1h
  url: {{ .Values.flux.helmRepository.url }}
  type: default
