apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-values
  namespace: pihole
data:
  pihole.primary.host: "${pihole.primary.host}"
  pihole.replica1.host: "${pihole.replica1.host}"
  pihole.replica2.host: "${pihole.replica2.host}"
  pihole.replica3.host: "${pihole.replica3.host}"
