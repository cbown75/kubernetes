apiVersion: apps/v1
kind: Deployment
metadata:
  name: nebula-sync-pihole1
  namespace: pihole
  labels:
    app: nebula-sync-pihole1
    instance: pihole1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nebula-sync-pihole1
  template:
    metadata:
      labels:
        app: nebula-sync-pihole1
        instance: pihole1
    spec:
      initContainers:
        # Build PRIMARY and REPLICAS as files that nebula-sync can read
        - name: setup-config
          image: busybox:1.36
          command: ['sh', '-c']
          args:
            - |
              echo "Setting up nebula-sync configuration..."

              # Read passwords from mounted secrets
              PRIMARY_PASS=$(cat /secrets/primary-password)
              REPLICA1_PASS=$(cat /secrets/replica-password-1)
              REPLICA2_PASS=$(cat /secrets/replica-password-2)
              REPLICA3_PASS=$(cat /secrets/replica-password-3)

              # Create files for PRIMARY_FILE and REPLICAS_FILE environment variables
              echo "http://pihole1.home.cwbtech.net|${PRIMARY_PASS}" > /config/primary
              echo "http://pihole2.home.cwbtech.net|${REPLICA1_PASS},http://pihole7.home.cwbtech.net|${REPLICA2_PASS},http://pihole8.home.cwbtech.net|${REPLICA3_PASS}" > /config/replicas

              echo "Configuration complete"
          volumeMounts:
            - name: secrets
              mountPath: /secrets
              readOnly: true
            - name: config
              mountPath: /config
      containers:
        - name: nebula-sync
          image: ghcr.io/lovelaze/nebula-sync:latest
          imagePullPolicy: Always
          env:
            # Use file-based configuration
            - name: PRIMARY_FILE
              value: "/config/primary"
            - name: REPLICAS_FILE
              value: "/config/replicas"
            # Scheduling
            - name: CRON
              value: "*/5 * * * *" # Every 5 minutes
            - name: TZ
              value: "America/Phoenix"
            # Sync mode
            - name: FULL_SYNC
              value: "false"
            # Sync configuration options
            - name: SYNC_CONFIG_DNS
              value: "true"
            - name: SYNC_CONFIG_DHCP
              value: "true"
            - name: SYNC_CONFIG_NTP
              value: "true"
            - name: SYNC_CONFIG_RESOLVER
              value: "true"
            - name: SYNC_CONFIG_DATABASE
              value: "true"
            - name: SYNC_CONFIG_MISC
              value: "false"
            - name: SYNC_CONFIG_DEBUG
              value: "false"
            # Gravity sync options
            - name: SYNC_GRAVITY_DHCP_LEASES
              value: "true"
            - name: SYNC_GRAVITY_GROUP
              value: "true"
            - name: SYNC_GRAVITY_AD_LIST
              value: "true"
            - name: SYNC_GRAVITY_AD_LIST_BY_GROUP
              value: "true"
            - name: SYNC_GRAVITY_DOMAIN_LIST
              value: "true"
            - name: SYNC_GRAVITY_DOMAIN_LIST_BY_GROUP
              value: "true"
            - name: SYNC_GRAVITY_CLIENT
              value: "true"
            - name: SYNC_GRAVITY_CLIENT_BY_GROUP
              value: "true"
            # Run gravity after sync
            - name: RUN_GRAVITY
              value: "true"
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
      volumes:
        - name: secrets
          secret:
            secretName: nebula-sync-pihole1-secrets
            defaultMode: 0400
        - name: config
          emptyDir: {}
