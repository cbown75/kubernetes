apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Core infrastructure (order matters for dependencies)
  - node-labels
  - storage
  - sealed-secrets
  - cert-manager
  - traefik
  - prometheus
